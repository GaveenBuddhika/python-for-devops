import os
# we need to import os then use commandline argument to add the env variable
#cmd arg =  $env:password (variable name can change) = "gaveen"




#we can print it using this
print (os.getenv( "password" ) )